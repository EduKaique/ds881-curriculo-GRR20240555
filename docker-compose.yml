services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:4321"
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm run dev -- --host 0.0.0.0 --port 4321
    environment:
      - NODE_ENV=development
